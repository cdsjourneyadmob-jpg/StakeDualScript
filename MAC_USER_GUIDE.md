# 🍎 Mac User Guide - StakeDualScript

## 📥 What You Receive:
- `stakedualscript-macos` (executable file)
- License key (e.g., `CUSTOMER-WEEKLY-001`)
- This setup guide

## 🚀 Step-by-Step Setup:

### Step 1: Prepare the Bot
```bash
# Open Terminal (Cmd + Space, type "Terminal")
# Navigate to where you saved the file
cd ~/Downloads

# Make the file executable
chmod +x stakedualscript-macos
```

### Step 2: Run the Bot
```bash
# Run the bot
./stakedualscript-macos
```

### Step 3: Enter License Key
When prompted, enter your license key exactly as provided:
```
Enter your license key: CUSTOMER-WEEKLY-001
```

### Step 4: Bot Starts
You'll see:
```
✅ License Valid! Starting StakeDualScript Bot...

🎯 Bot Configuration:
   Strategy: Dual-Game Automation (Crash + Slide)
   Base Bet: 0.02 INR
   Normal Cashout: 1.25x (80% win rate)
   ...
```

### Step 5: Configure Cookies
Follow the bot's instructions to:
1. Login to Stake.com
2. Copy cookies from browser
3. Update bot configuration
4. Start earning!

## 🛠️ Troubleshooting:

**"Permission denied"**
```bash
chmod +x stakedualscript-macos
```

**"Cannot be opened because it is from an unidentified developer"**
```bash
# Right-click the file → Open → Open anyway
# Or use Terminal:
xattr -d com.apple.quarantine stakedualscript-macos
```

**"Invalid license key"**
- Check spelling exactly
- Contact support if correct

## 📞 Support:
- **Email**: harryverma0002@example.com
- **Telegram**: @YourTelegramHandle

## 💰 Start Earning:
Once configured, your bot runs 24/7 automatically generating profits!